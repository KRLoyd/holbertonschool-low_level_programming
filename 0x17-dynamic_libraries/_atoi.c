#include "holberton.h"
#include <stdio.h>
/**
 * _atoi - converts a string to an integer
 * @s: string to evaluate
 *
 * Return: Always -1, not functioning yet
 */
int _atoi(char *s)
{
	printf("%s\n", s);
	return (-1);
}
